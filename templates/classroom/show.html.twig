{% extends 'base.html.twig' %}

{% block title %}Classroom
{% endblock %}

{% block body %}
	<h1>Classroom</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Ref</th>
				<td>{{ classroom.ref }}</td>
			</tr>
			<tr>
				<th>Name</th>
				<td>{{ classroom.name }}</td>
			</tr>
			<tr>
				<th>CreatedAt</th>
				<td>{{ classroom.createdAt ? classroom.createdAt|date('Y-m-d H:i:s') : '' }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_classroom_index') }}">retour</a>

	<a href="{{ path('app_classroom_edit', {'ref': classroom.ref}) }}">edit</a>

	{{ include('classroom/_delete_form.html.twig') }}
{% endblock %}
